---
title: "浏览器渲染原理及优化 (): 关键渲染路径"
date: 2026-06-01
tags: ["语雀"]
source_kind: yuque
---

当浏览器从服务端接收到⼀个HTML⻚⾯的请求时，屏幕上渲染出来要经过很多个步骤。浏览器完成⼀
系列的运⾏完成渲染到我们可视的内容通常称之为"关键路径渲染"(Critical Rendering Path)
理解CRP相关的知识可以帮助我们更好的提供⽹站的性能，主要分为以下⼏个部分
构建DOM树
构建CSSOM树
运⾏JS
创建Render树
⽣成布局Layout
绘制Paint
合成图像(Composite)●
●
●
●
●
●
●

146这⼀步其实⽐较简单，我们基本也都知道，主要流程如下图所示：
css转化为CSSOM的处理过程和HTML有些类似，只不过是CSS⽽不是HTML
CSS字节转化为字符，经过上述的过程，最终⽣成CSSOM树的结构1、将HTML转化为DOM
2、将CSS转化为CSSOM

147body节点的⼦节点继承了body的样式规则(font-size: 16px)，这就是CSS为啥称之为层叠样式表。
注：HTML可以逐步解析，它不需要等待所有DOM都构建完毕后再去构建CSSOM，⽽是在解析HTML
构建DOM时，若遇⻅CSS会⽴刻构建CSSOM，它们可以同时进⾏。
然⽽CSS需要完整的构建，不完整的CSS是⽆法使⽤的，因为CSS的每个属性都可以改变CSSOM，所
以会存在这样⼀个问题：假设前⾯⼏个字节的CSS将字体⼤⼩设置为16px，后⾯⼜将字体⼤⼩设置为
14px，那么如果不把整个CSSOM构建完整，最终得到的CSSOM其实是不准确的。
因⽽必须等CSSOM构建完毕才能进⼊到下⼀个阶段，哪怕DOM已经构建完，它也得等CSSOM，然后才能进⼊
下⼀个阶段。所以CSS也被认为是“渲染阻塞资源”
我们都知道JavaScript的加载、解析与执⾏会阻塞DOM的构建，也就是说，在构建DOM时，HTML解析
器若遇到了JavaScript，那么它会暂停构建DOM，将控制权移交给JavaScript引擎，等JavaScript引擎
运⾏完毕，浏览器再从中断的地⽅恢复DOM构建。因⽽JavaScript也被认为是⼀个"解析器阻塞资源"。
同时JS可能会改变DOM结构，但JS⽆法获取到JS后⾯的元素，因为DOM还没有构建到那边。JS对关键
路径渲染的影响不只是阻塞DOM构建，也会导致CSSOM阻塞DOM构建，这是为什么呢？
原本DOM和CSSOM的构建是互不影响，但是⼀旦引⼊了JavaScript之后，JS如果需要去更改
CSSOM。前⾯我们说过了不完整的CSSOM是⽆法使⽤的，但是JS想去访问CSSOM并更改它，那么在
执⾏JS前，我们就需要拿到完整的CSSOM。这样就导致了⼀个情况：如果浏览器尚未完成CSSOM的3、运⾏JS

148下载和构建，⽽我们却想在此时运⾏脚本，那么浏览器将延迟脚本执⾏和DOM构建，直⾄其完成
CSSOM的下载和构建。
换句话说在这种情况下，浏览器会先下载和构建CSSOM，然后再执⾏JavaScript，最后在继续构建DOM。这
样就会导致严重的性能问题，如下图
Render树是DOM和CSSOM的组合，这个树代表了⻚⾯最后的呈现。4、创建Render树图⽚加载失败

149渲染树的重要特性是它捕获可⻅的内容，有些节点被CSS隐藏在渲染树中就会被忽略，如上图中的p > 
span节点就不会出现在渲染树中，因为该阶段设置了display:none的属性。
对于每个可⻅的节点，都会为其适配CSSOM规则并应⽤它们
有了渲染树之后，接下来就会进⼊布局阶段，就是浏览器依据你的各个节点在⻚⾯中确切的位置和⼤
⼩，通常这⼀⾏为也被称为“⾃动重排”。
布局流程就是输⼊⼀个"盒模型"，它会精确地捕获每个元素在视⼝内的确切位置和尺⼨，所有相对测量值都
将转换为屏幕上的绝对像素。如下图：5、布局Layout图⽚加载失败

150当布局完成的时候，光栅器就会执⾏绘制操作(类似canvas画布的绘制过程)，去填充⻚⾯的像素。过程
如下所示：5、绘制Paint图⽚加载失败

151我们知道PS⾥⾯的⼀张图⽚是有多个图层合成的，其实⽹⻚也是⼀样的，当绘制完成之后，就会将多个
绘制的图层进⾏合并，最终现在在我们浏览器屏幕上的就是合成后的⽹⻚，通过chrome的⼯具我们可以
看到这样的视图结构6、合成图层Composite

152参考：
关键路径优化： https://github.com/berwin/Blog/issues/29
优达学城：浏览器渲染优化(Google出品)

153