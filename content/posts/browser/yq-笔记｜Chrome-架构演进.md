---
title: "笔记｜Chrome 架构演进"
date: 2020-04-10
tags: ["语雀"]
source_kind: yuque
---

单进程浏览器是指浏览器的所有功能模块都是运行在同一个进程里面，这些模块包含了网络、插件、
JavaScript的运行环境、渲染引擎、页面等等，这种单进程的模型有以下缺点：
不稳定：早期浏览器需要借助于插件来实现诸如 Web 视频、Web 游戏等各种强大的功能，但是插
件是最容易出问题的模块，并且还运行在浏览器进程之中，所以一个插件的意外崩溃会引起整个浏
览器的崩溃
不流畅：从上面的“单进程浏览器架构示意图”可以看出，所有页面的渲染模块、JavaScript 执行环
境以及插件都是运行在同一个线程中的，这就意味着同一时刻只能有一个模块可以执行
不安全：1、插件  可以使用 C/C++ 等代码编写，通过插件可以获取到操作系统的任意资源，当你
在页面运行一个插件时也就意味着这个插件能完全操作你的电脑。如果是个恶意插件，那么它就可
以释放病毒、窃取你的账号密码，引发安全性问题。2、页面脚本  它可以通过浏览器的漏洞来获取
系统权限，这些脚本获取系统权限之后也可以对你的电脑做一些恶意的事情，同样也会引发安全问
题。1、单进程浏览器时代
- 
- 
- 
2、Chrome早期多进程架构

249我们在Chrome访问的页面会单独运行在渲染进程中，同时页面的插件也是运行在单独的插件进程中，
并且这两个进程是在沙箱中运行的，这样即使在渲染进程或者插件进程里面执行了恶意程序，恶意程序
也无法突破沙箱去获取系统权限来保证了安全。进程之间则是通过IPC机制来通信的(图中虚线部分)
浏览器进程： 主要负责界面显示、用户交互、子进程管理，同时提供存储等功能3、目前多进程架构
- 

250渲染进程：核心任务是将 HTML、CSS 和 JavaScript 转换为用户可以与之交互的网页，排版引擎 
Blink 和 JavaScript 引擎 V8 都是运行在该进程中，默认情况下，Chrome 会为每个 Tab 标签创
建一个渲染进程。出于安全考虑，渲染进程都是运行在沙箱模式下
GPU进程：Chrome 刚开始发布的时候是没有 GPU 进程的，而 GPU 的使用初衷是为了实现 3D 
CSS 的效果，只是随后网页、Chrome 的 UI 界面都选择采用 GPU 来绘制，这使得 GPU 成为浏
览器普遍的需求。最后，Chrome 在其多进程架构上也引入了 GPU 进程。
网络进程：主要负责页面的网络资源加载，之前是作为一个模块运行在浏览器进程里面的，直至最
近才独立出来，成为一个单独的进程。
插件进程：主要是负责插件的运行，因插件易崩溃，所以需要通过插件进程来隔离，以保证插件进
程崩溃不会对浏览器和页面造成影响。
缺点：
更高的资源占用，因为每个进程都会包含公共基础结构的副本（如 JavaScript 运行环境），这就意味
着浏览器会消耗更多的内存资源
更复杂的体系架构。浏览器各模块之间耦合性高、扩展性差等问题，会导致现在的架构已经很难适应新
的需求了
Chrome 最终要把 UI、数据库、文件、设备、网络等模块重构为基础服务，类似操作系统底层服务，下
面是 Chrome“面向服务的架构”的进程模型图●
- 
- 
- 
4、未来面向服务的架构

251通过chrome的任务管理器我们就可以查看当前进程，基础的服务进程有GPU、网络进程、音频服务进
程。
同时Chrome还提供灵活的弹性架构，在强大性能设备上会以多进程的方式运行基础服务，但是如果在
资源受限的设备上（如下图），Chrome 会将很多服务整合到一个进程中，从而节省内存占用。

253译 | JavaScript 是如何运行：事件循 环和异步编
程的兴起以及  个更好的使用 async/await 的技巧
(上)
现在，我们将通过回顾第一篇文章中的单线程环境下的弊端以及如何去克服它们以构建更棒的UI界面和
交互来展开写。按照习惯，在本文的最后将分享5条关于如何使用async/await来编写更清晰的代码技巧
在第一篇文章中，我们就仔细的考虑了一下问题：当你的调用栈中的函数执行需要消耗大量的时间会发
生什么？
比如，试着想象一下在浏览器中去惊醒复杂的图像转换算法
在调用栈的函数执行的时候，此时浏览器将不能做任何事情-被阻塞了。这意味着浏览器不能去渲染了
和执行接下来的任何代码。这样你的UI将不在具有很流畅的体验了
程序运行被卡住了
在某些情况下，这可能不是一个很重要的问题。但是还有一个更严重的问题是，当有太多任务需要去在
浏览器的调用栈中运行的时候，它可能长时间的停止响应。那是大部分的浏览器会发抛出一个错误，询
问你是否关闭当前浏览的网页
这是相当糟糕的，会影响到用户体验为什么单线程是有局限性的？

254你可能在单个js文件中写你的程序，但你的程序基本上是由一些代码模块组成的，某个时刻仅一个代码
块去执行，其他的在后面去执行，最常见的代码块是函数。
大部分新手JavaScript开发者的理解是之后运行并不是必须严格的在现在之后立即执行。换一种方式
说，现在无法完成的任务将会用异步的方式去执行。这样你的潜意识的理解就不会去遇到上面提到的提
到的浏览器/UI阻塞的行为
让我们看下面的例子：
你或许知道标准的ajax请求无法同步的去完成，意味着当代码执行的时候，ajax(...)还没有拿到返回值给
response变量赋值
等待异步函数返回结果的一种简单方式是去使用回调函数：JavaScript程序模块
// ajax(..) is some arbitrary Ajax function given by a library
var response  = ajax('https://example.com/api' );
console.log(response );
// `response` won't have the response1
JavaScript

255注意：实际上我们是可以去发送同步的ajax请求，但永远不要那么去做。如果你是发送了同步的ajax请
求，程序的UI交互将被阻塞，用户将无法点击、输入数据、控制导航或者滚动界面。用户无法完成交互
操作，是十分糟糕的体验。
以下就是实例代码，请永远不要这么做
上面是用ajax作为请求的例子，你也可以让其他的代码块去异步的执行
可以使用setTimeOut(callback, milliseconds)函数，setTimeOut的作用是在之后的某个时间去触发事
件。代码如下：ajax('https://example.com/api' , function (response ) {
    console.log(response );  // `response` is now available
});1
JavaScript
// This is assuming that you're using jQuery
jQuery.ajax({
    url: 'https://api.example.com/endpoint' ,
    success: function (response ) {
        // This is your callback.
    },
    async: false // And this is a terrible idea
});1
JavaScript

256在控制台输出的结果是：
我们将从一个让人费解的问题开始 - 尽管JavaScript是允许异步的执行代码(比如前面说的
setTimeOut)，事实上直到ES6，JavaScript本身是没有内置任何异步的概念，在给定的某个时刻JS引
擎除了执行单个代码块之外没有做任何其他的事情
那么JS引擎是如何执行程序片段的呢？实际上JS引擎并不是单独运行的，它拥有用一个宿主环境。对于
大多数的开发者而言宿主环境是web浏览器或者Node.js，实际上JavaScript现在已经嵌入到从电灯到机
器人等各种硬件中了，每个设备内置的JS引擎的宿主环境类型也不一样。
所有宿主环境的相同点拥有一个被称为事件循环的内置机制，该机制可以在美的调用JS引擎的时候去处
理一段时间内的多个程序块
这意味着JS引擎仅仅是任何JS代码的一个执行环境，有一个外部的环境去调度JS代码执行事件
举个例子，当你的JavaScript程序去执行Ajax请求从服务器获取数据的时候，你可以在回调函数中去设
置"respone"代码处理，这时JS引擎将回去告诉宿主环境："嘿，我现在将挂起执行，当网络请求完成的剖析事件循环function  first() {
    console.log('first');
}
function  second() {
    console.log('second' );
}
function  third() {
    console.log('third');
}
first();
setTimeout (second, 1000);  // Invoke `second` after 1000ms
third();1
JavaScript
first
third
second1
JavaScript

257时候并返回了响应数据，请执行回调函数"
浏览器会监听从网络中返回的数据，当数据返回的时候，它会把回调函数push到事件循环的机制内调度
执行
过程如下图所示
上面是Web APIs呢？本质上你没有权限去访问这些线程，只能够去调用它们。他们是浏览器自带的，并
且可以进行并发操作。如果你是Node.js开发者，这些就是C++ APIs了
那么事件循环到底是什么呢？

258简单的说事件循环的运行机制是：监测调用栈和回调队列，如果调用栈是空的，它将第一个事件从回调
队列中push到调用栈，从而有效的去运行。
在事件循环中这样的一次迭代被称为一个tick，每个事件是一个回调函数
让我们执行这段代码，看会发生什么
1、空白状态。浏览器控制台是空的，调用栈是空的。console.log('Hi');
setTimeout (function  cb1() { 
    console.log('cb1');
}, 5000);
console.log('Bye');1
JavaScript

2592、console.log('Hi')是被push到调用栈中
3、console.log('Hi')被执行

2604、console.log('Hi')出栈

2615、setTimeOut(function cb1(){...}) 入栈

2626、setTimeOut被执行，浏览器会为其创建一个定时器，并且帮你处理倒计时。

2637、setTimeOut执行完毕并且出栈

2648、console.log('Bye')入栈

2659、console.log('Bye')被执行

26610、console.log('Bye')出栈

26711、在不少于5s后，定时器结束运行并将回调函数cb1 push到回调队列中

26812、事件循环从回调队列中获取函数cb1并将其推入调用栈用

26913、cb1被执行同时console.log('cb1')入栈

27014、console.log('cb1')执行

27115、console.log('cb1')出栈

27216、回调函数cb1出栈

273执行的动画过程如下

274值得注意的是在ES6中规定了事件循环改如何去运行，这意味着从技术上而言，这已经属于JS引擎职责
内的范围了，而不需要依托宿主环境。进行此更改的主要原因之一是在ES6中引入了Promises，因为后
者需要对事件循环队列上的调度操作进行直接，细粒度的控制（我们将在后面详细讨论）。
需要着重注意的是，setTimeOut(...)不会自动的将回调函数放到回调队列中。它设置了一个定时器，当
时间到期了，宿主环境(chrome浏览器、node等)会将回调函数放到事件循环中，在将来的某个Tick取出
并执行代码。看下面的代码示例
这并不意味着1000ms后会执行myCallback，1000ms后回调函数myCallback会被添加到回调队列中，
但是队列中还可能有其他更早添加进来的事件，这样你的回调将进入等待状态setTimeOut(...)运行原理
setTimeout (myCallback , 1000); 1
JavaScript

275有许多的文章和教程开始会建议你去使用setTimeOut(callback, 0)来书写 JavaScript 异步代
码。现在你明白了事件循环和setTimeOut的原理：调用setTimeOut的时候将第二个参数设置为0
表示延迟回调执行知道调用栈被清除。
代码如下
通过设置等待时间是0，浏览器控制台打印的结果是：
下部分将讲解Event Loop在ES6中是如何运行的console.log('Hi');
setTimeout (function () {
    console.log('callback' );
}, 0);
console.log('Bye');1
JavaScript
Hi
Bye
callback1
JavaScript

276译 | JavaScript 是如何运行：引擎， 运行时和调
用堆栈的概述
这是How JavaScript works的第一篇，部分个人觉得不重要的点有删减，英文原文请访问 
https://blog.sessionstack.com/how-does-javascript-actually-work-part-1-b0bacc073cf
⼏乎所有的人都听过V8引擎，大部分的人也都知道JavaScript是单线程的或者使用了回调队列。
在这篇文章中，我们将详细的讲述这些概率，并解释JavaScript如何实际的运行。通过知道的这些细
节，你将可以正确的利用提供的API写出更好的、非阻塞的程序
如果你是JavaScript的新手，这篇文章可以帮助你更好的理解为什么JavaScript相比于其他的语言如此
的"古怪"
如果你是有经验丰富的JavaScript开发人员，希望在日常的工作中，它将会给你带来一些新的见解有关
JavaScript运行时的工作原理
谷歌的V8引擎是最受欢迎的一个JavaScript引擎。V8在Chrome浏览器和Node.js中被运用，下面是一
个简化的视图模型概述
JavaScript引擎

277V8引擎主要包含两个部分
动态内存管理—— 负责分配内存
调用栈——这是代码执行时堆栈帧所在的位置
⼏乎每个JavaScript开发者都使用过浏览器的APIs(比如，setTimeOut)，但是他们并不是被JS引擎提供
的
那么它们从那里来的呢？
实际上情况有点复杂●
- 
运行时

278实际上除了JS引擎，浏览器还给我提供了一些Web APIs，比如DOM, AJAX, setTimeOut等等 
然后，还有了广为人知的事件循环和回调队列
JavaScript是单线程的语言，意味着只有单个的调用栈，这样一次只能做一件事情。
调用堆栈是一种数据结构，里面会大体上记录了在程序在执行中的位置。  当执行进入一个函数，就把
它置于栈的顶部。如果函数return执行结束，我们就将栈的顶部移除栈。这就是栈结构的工作原理
举个例子，我们看下下面的代码调用栈

279当JS引擎开始执行代码的时候，调用栈是空的。之后，步骤如下
调用栈的每个入口被称为堆栈结构
当有一个异常被抛出时这正是堆栈追踪的构造方式 - 当异常发生的时候基本上就是调用栈的状态，我们
看下面的代码function  multiply (x, y) {
    return x * y;
}
function  printSquare (x) {
    var s = multiply (x, x);
    console.log(s);
}
printSquare (5);1
JavaScript

280在Chrome中执行(代码对应的文件命名为foo.js)，将会产生如下的堆栈追踪： 
"栈溢出" — 当超过最大调用栈的大小就会发生栈溢出。这种情况很容易发生，特别是在代码中使用递
归没有测试你的代码的时候，下面有个简单的例子:
当JS引擎执行这段代码时，“foo”函数被调用。然而，会递归并开始调用其自身而没有任何结束的条
件。因此每次执行的时候，相同的函数会被添加到调用栈中直到栈溢出，过程就像下面这样：function  foo() {
    throw new Error('SessionStack will help you resolve crashes :)' );
}
function  bar() {
    foo();
}
function  start() {
    bar();
}
start();1
JavaScript
function  foo() {
    foo();
}
foo();1
JavaScript

281在某个时刻，调用堆栈中的函数调用次数超过了调用堆栈的实际大小，这样浏览器就会抛出错误的
行为，通过看到的错误信息，如下：
代码在单线程中运行是相当舒服的，我们不需要去考虑多线程环境下产生的一些复杂情况，比如
死锁
但是单线程运行也很收限制，由于JavaScript是单线程的，当它执行很慢的该怎么办呢?
当你的在调用栈中执行函数的花费的时间很长会发生什么？例如你想在浏览器中用JavaScript执行一些
复杂的图像转化
你也许会问-这也是一个问题？ 这问题是当你的调用栈中函数在执行的时候，浏览器实际上做不了任何
事情-它被阻塞了。这意味着浏览器将不能去渲染，不能够去运行其他的任何代码，它卡住了。如果你
想去在你的app中有更流畅体验的这就是个问题并发 & 事件循环

282并且这不是唯一的问题。一旦你的浏览器开始在调用栈中执行多个任务，它可能在很长一段时间内停止
响应。并且浏览器会抛出一个错误，询问你是否关闭这个网页。
现在，这不是最好的用户体验，是嘛？
那么，我们如何在不阻塞UI和浏览器无响应的情况下去执行一些笨重的代码呢？这个解决方式是异步回
调
