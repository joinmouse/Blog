---
title: "笔记｜Chrome 架构演进"
date: 2026-06-01
tags: ["语雀"]
source_kind: yuque
---

单进程浏览器是指浏览器的所有功能模块都是运⾏在同⼀个进程⾥⾯，这些模块包含了⽹络、插件、
JavaScript的运⾏环境、渲染引擎、⻚⾯等等，这种单进程的模型有以下缺点：
不稳定：早期浏览器需要借助于插件来实现诸如 Web 视频、Web 游戏等各种强⼤的功能，但是插
件是最容易出问题的模块，并且还运⾏在浏览器进程之中，所以⼀个插件的意外崩溃会引起整个浏
览器的崩溃
不流畅：从上⾯的“单进程浏览器架构示意图”可以看出，所有⻚⾯的渲染模块、JavaScript 执⾏环
境以及插件都是运⾏在同⼀个线程中的，这就意味着同⼀时刻只能有⼀个模块可以执⾏
不安全：1、插件  可以使⽤ C/C++ 等代码编写，通过插件可以获取到操作系统的任意资源，当你
在⻚⾯运⾏⼀个插件时也就意味着这个插件能完全操作你的电脑。如果是个恶意插件，那么它就可
以释放病毒、窃取你的账号密码，引发安全性问题。2、⻚⾯脚本  它可以通过浏览器的漏洞来获取
系统权限，这些脚本获取系统权限之后也可以对你的电脑做⼀些恶意的事情，同样也会引发安全问
题。1、单进程浏览器时代
●
●
●
2、Chrome早期多进程架构

249我们在Chrome访问的⻚⾯会单独运⾏在渲染进程中，同时⻚⾯的插件也是运⾏在单独的插件进程中，
并且这两个进程是在沙箱中运⾏的，这样即使在渲染进程或者插件进程⾥⾯执⾏了恶意程序，恶意程序
也⽆法突破沙箱去获取系统权限来保证了安全。进程之间则是通过IPC机制来通信的(图中虚线部分)
浏览器进程： 主要负责界⾯显示、⽤户交互、⼦进程管理，同时提供存储等功能3、⽬前多进程架构
●

250渲染进程：核⼼任务是将 HTML、CSS 和 JavaScript 转换为⽤户可以与之交互的⽹⻚，排版引擎 
Blink 和 JavaScript 引擎 V8 都是运⾏在该进程中，默认情况下，Chrome 会为每个 Tab 标签创
建⼀个渲染进程。出于安全考虑，渲染进程都是运⾏在沙箱模式下
GPU进程：Chrome 刚开始发布的时候是没有 GPU 进程的，⽽ GPU 的使⽤初衷是为了实现 3D 
CSS 的效果，只是随后⽹⻚、Chrome 的 UI 界⾯都选择采⽤ GPU 来绘制，这使得 GPU 成为浏
览器普遍的需求。最后，Chrome 在其多进程架构上也引⼊了 GPU 进程。
⽹络进程：主要负责⻚⾯的⽹络资源加载，之前是作为⼀个模块运⾏在浏览器进程⾥⾯的，直⾄最
近才独⽴出来，成为⼀个单独的进程。
插件进程：主要是负责插件的运⾏，因插件易崩溃，所以需要通过插件进程来隔离，以保证插件进
程崩溃不会对浏览器和⻚⾯造成影响。
缺点：
更⾼的资源占⽤，因为每个进程都会包含公共基础结构的副本（如 JavaScript 运⾏环境），这就意味
着浏览器会消耗更多的内存资源
更复杂的体系架构。浏览器各模块之间耦合性⾼、扩展性差等问题，会导致现在的架构已经很难适应新
的需求了
Chrome 最终要把 UI、数据库、⽂件、设备、⽹络等模块重构为基础服务，类似操作系统底层服务，下
⾯是 Chrome“⾯向服务的架构”的进程模型图●
●
●
●
4、未来⾯向服务的架构

251通过chrome的任务管理器我们就可以查看当前进程，基础的服务进程有GPU、⽹络进程、⾳频服务进
程。
同时Chrome还提供灵活的弹性架构，在强⼤性能设备上会以多进程的⽅式运⾏基础服务，但是如果在
资源受限的设备上（如下图），Chrome 会将很多服务整合到⼀个进程中，从⽽节省内存占⽤。

253译 | JavaScript 是如何运⾏：事件循 环和异步编
程的兴起以及  个更好的使⽤ async/await 的技巧
(上)
现在，我们将通过回顾第⼀篇⽂章中的单线程环境下的弊端以及如何去克服它们以构建更棒的UI界⾯和
交互来展开写。按照习惯，在本⽂的最后将分享5条关于如何使⽤async/await来编写更清晰的代码技巧
在第⼀篇⽂章中，我们就仔细的考虑了⼀下问题：当你的调⽤栈中的函数执⾏需要消耗⼤量的时间会发
⽣什么？
⽐如，试着想象⼀下在浏览器中去惊醒复杂的图像转换算法
在调⽤栈的函数执⾏的时候，此时浏览器将不能做任何事情-被阻塞了。这意味着浏览器不能去渲染了
和执⾏接下来的任何代码。这样你的UI将不在具有很流畅的体验了
程序运⾏被卡住了
在某些情况下，这可能不是⼀个很重要的问题。但是还有⼀个更严重的问题是，当有太多任务需要去在
浏览器的调⽤栈中运⾏的时候，它可能⻓时间的停⽌响应。那是⼤部分的浏览器会发抛出⼀个错误，询
问你是否关闭当前浏览的⽹⻚
这是相当糟糕的，会影响到⽤户体验为什么单线程是有局限性的？

254你可能在单个js⽂件中写你的程序，但你的程序基本上是由⼀些代码模块组成的，某个时刻仅⼀个代码
块去执⾏，其他的在后⾯去执⾏，最常⻅的代码块是函数。
⼤部分新⼿JavaScript开发者的理解是之后运⾏并不是必须严格的在现在之后⽴即执⾏。换⼀种⽅式
说，现在⽆法完成的任务将会⽤异步的⽅式去执⾏。这样你的潜意识的理解就不会去遇到上⾯提到的提
到的浏览器/UI阻塞的⾏为
让我们看下⾯的例⼦：
你或许知道标准的ajax请求⽆法同步的去完成，意味着当代码执⾏的时候，ajax(...)还没有拿到返回值给
response变量赋值
等待异步函数返回结果的⼀种简单⽅式是去使⽤回调函数：JavaScript程序模块
// ajax(..) is some arbitrary Ajax function given by a library
var response  = ajax('https://example.com/api' );
console.log(response );
// `response` won't have the response1
3
5
JavaScript

255注意：实际上我们是可以去发送同步的ajax请求，但永远不要那么去做。如果你是发送了同步的ajax请
求，程序的UI交互将被阻塞，⽤户将⽆法点击、输⼊数据、控制导航或者滚动界⾯。⽤户⽆法完成交互
操作，是⼗分糟糕的体验。
以下就是实例代码，请永远不要这么做
上⾯是⽤ajax作为请求的例⼦，你也可以让其他的代码块去异步的执⾏
可以使⽤setTimeOut(callback, milliseconds)函数，setTimeOut的作⽤是在之后的某个时间去触发事
件。代码如下：ajax('https://example.com/api' , function (response ) {
    console.log(response );  // `response` is now available
});1
3
JavaScript
// This is assuming that you're using jQuery
jQuery.ajax({
    url: 'https://api.example.com/endpoint' ,
    success: function (response ) {
        // This is your callback.
    },
    async: false // And this is a terrible idea
});1
3
5
7
JavaScript

256在控制台输出的结果是：
我们将从⼀个让⼈费解的问题开始 - 尽管JavaScript是允许异步的执⾏代码(⽐如前⾯说的
setTimeOut)，事实上直到ES6，JavaScript本身是没有内置任何异步的概念，在给定的某个时刻JS引
擎除了执⾏单个代码块之外没有做任何其他的事情
那么JS引擎是如何执⾏程序⽚段的呢？实际上JS引擎并不是单独运⾏的，它拥有⽤⼀个宿主环境。对于
⼤多数的开发者⽽⾔宿主环境是web浏览器或者Node.js，实际上JavaScript现在已经嵌⼊到从电灯到机
器⼈等各种硬件中了，每个设备内置的JS引擎的宿主环境类型也不⼀样。
所有宿主环境的相同点拥有⼀个被称为事件循环的内置机制，该机制可以在美的调⽤JS引擎的时候去处
理⼀段时间内的多个程序块
这意味着JS引擎仅仅是任何JS代码的⼀个执⾏环境，有⼀个外部的环境去调度JS代码执⾏事件
举个例⼦，当你的JavaScript程序去执⾏Ajax请求从服务器获取数据的时候，你可以在回调函数中去设
置"respone"代码处理，这时JS引擎将回去告诉宿主环境："嘿，我现在将挂起执⾏，当⽹络请求完成的剖析事件循环function  first() {
    console.log('first');
}
function  second() {
    console.log('second' );
}
function  third() {
    console.log('third');
}
first();
setTimeout (second, 1000);  // Invoke `second` after 1000ms
third();1
3
5
7
9
11
JavaScript
first
third
second1
3
JavaScript

257时候并返回了响应数据，请执⾏回调函数"
浏览器会监听从⽹络中返回的数据，当数据返回的时候，它会把回调函数push到事件循环的机制内调度
执⾏
过程如下图所示
上⾯是Web APIs呢？本质上你没有权限去访问这些线程，只能够去调⽤它们。他们是浏览器⾃带的，并
且可以进⾏并发操作。如果你是Node.js开发者，这些就是C++ APIs了
那么事件循环到底是什么呢？

258简单的说事件循环的运⾏机制是：监测调⽤栈和回调队列，如果调⽤栈是空的，它将第⼀个事件从回调
队列中push到调⽤栈，从⽽有效的去运⾏。
在事件循环中这样的⼀次迭代被称为⼀个tick，每个事件是⼀个回调函数
让我们执⾏这段代码，看会发⽣什么
1、空⽩状态。浏览器控制台是空的，调⽤栈是空的。console.log('Hi');
setTimeout (function  cb1() { 
    console.log('cb1');
}, 5000);
console.log('Bye');1
3
5
JavaScript

2592、console.log('Hi')是被push到调⽤栈中
3、console.log('Hi')被执⾏

2604、console.log('Hi')出栈

2615、setTimeOut(function cb1(){...}) ⼊栈

2626、setTimeOut被执⾏，浏览器会为其创建⼀个定时器，并且帮你处理倒计时。

2637、setTimeOut执⾏完毕并且出栈

2648、console.log('Bye')⼊栈

2659、console.log('Bye')被执⾏

26610、console.log('Bye')出栈

26711、在不少于5s后，定时器结束运⾏并将回调函数cb1 push到回调队列中

26812、事件循环从回调队列中获取函数cb1并将其推⼊调⽤栈⽤

26913、cb1被执⾏同时console.log('cb1')⼊栈

27014、console.log('cb1')执⾏

27115、console.log('cb1')出栈

27216、回调函数cb1出栈

273执⾏的动画过程如下

274值得注意的是在ES6中规定了事件循环改如何去运⾏，这意味着从技术上⽽⾔，这已经属于JS引擎职责
内的范围了，⽽不需要依托宿主环境。进⾏此更改的主要原因之⼀是在ES6中引⼊了Promises，因为后
者需要对事件循环队列上的调度操作进⾏直接，细粒度的控制（我们将在后⾯详细讨论）。
需要着重注意的是，setTimeOut(...)不会⾃动的将回调函数放到回调队列中。它设置了⼀个定时器，当
时间到期了，宿主环境(chrome浏览器、node等)会将回调函数放到事件循环中，在将来的某个Tick取出
并执⾏代码。看下⾯的代码示例
这并不意味着1000ms后会执⾏myCallback，1000ms后回调函数myCallback会被添加到回调队列中，
但是队列中还可能有其他更早添加进来的事件，这样你的回调将进⼊等待状态setTimeOut(...)运⾏原理
setTimeout (myCallback , 1000); 1
JavaScript

275有许多的⽂章和教程开始会建议你去使⽤setTimeOut(callback, 0)来书写 JavaScript 异步代
码。现在你明⽩了事件循环和setTimeOut的原理：调⽤setTimeOut的时候将第⼆个参数设置为0
表示延迟回调执⾏知道调⽤栈被清除。
代码如下
通过设置等待时间是0，浏览器控制台打印的结果是：
下部分将讲解Event Loop在ES6中是如何运⾏的console.log('Hi');
setTimeout (function () {
    console.log('callback' );
}, 0);
console.log('Bye');1
3
5
JavaScript
Hi
Bye
callback1
3
JavaScript

276译 | JavaScript 是如何运⾏：引擎， 运⾏时和调
⽤堆栈的概述
这是How JavaScript works的第⼀篇，部分个⼈觉得不重要的点有删减，英⽂原⽂请访问 
https://blog.sessionstack.com/how-does-javascript-actually-work-part-1-b0bacc073cf
⼏乎所有的⼈都听过V8引擎，⼤部分的⼈也都知道JavaScript是单线程的或者使⽤了回调队列。
在这篇⽂章中，我们将详细的讲述这些概率，并解释JavaScript如何实际的运⾏。通过知道的这些细
节，你将可以正确的利⽤提供的API写出更好的、⾮阻塞的程序
如果你是JavaScript的新⼿，这篇⽂章可以帮助你更好的理解为什么JavaScript相⽐于其他的语⾔如此
的"古怪"
如果你是有经验丰富的JavaScript开发⼈员，希望在⽇常的⼯作中，它将会给你带来⼀些新的⻅解有关
JavaScript运⾏时的⼯作原理
⾕歌的V8引擎是最受欢迎的⼀个JavaScript引擎。V8在Chrome浏览器和Node.js中被运⽤，下⾯是⼀
个简化的视图模型概述
JavaScript引擎

277V8引擎主要包含两个部分
动态内存管理—— 负责分配内存
调⽤栈——这是代码执⾏时堆栈帧所在的位置
⼏乎每个JavaScript开发者都使⽤过浏览器的APIs(⽐如，setTimeOut)，但是他们并不是被JS引擎提供
的
那么它们从那⾥来的呢？
实际上情况有点复杂●
●
运⾏时

278实际上除了JS引擎，浏览器还给我提供了⼀些Web APIs，⽐如DOM, AJAX, setTimeOut等等 
然后，还有了⼴为⼈知的事件循环和回调队列
JavaScript是单线程的语⾔，意味着只有单个的调⽤栈，这样⼀次只能做⼀件事情。
调⽤堆栈是⼀种数据结构，⾥⾯会⼤体上记录了在程序在执⾏中的位置。  当执⾏进⼊⼀个函数，就把
它置于栈的顶部。如果函数return执⾏结束，我们就将栈的顶部移除栈。这就是栈结构的⼯作原理
举个例⼦，我们看下下⾯的代码调⽤栈

279当JS引擎开始执⾏代码的时候，调⽤栈是空的。之后，步骤如下
调⽤栈的每个⼊⼝被称为堆栈结构
当有⼀个异常被抛出时这正是堆栈追踪的构造⽅式 - 当异常发⽣的时候基本上就是调⽤栈的状态，我们
看下⾯的代码function  multiply (x, y) {
    return x * y;
}
function  printSquare (x) {
    var s = multiply (x, x);
    console.log(s);
}
printSquare (5);1
3
5
7
9
JavaScript

280在Chrome中执⾏(代码对应的⽂件命名为foo.js)，将会产⽣如下的堆栈追踪： 
"栈溢出" — 当超过最⼤调⽤栈的⼤⼩就会发⽣栈溢出。这种情况很容易发⽣，特别是在代码中使⽤递
归没有测试你的代码的时候，下⾯有个简单的例⼦:
当JS引擎执⾏这段代码时，“foo”函数被调⽤。然⽽，会递归并开始调⽤其⾃身⽽没有任何结束的条
件。因此每次执⾏的时候，相同的函数会被添加到调⽤栈中直到栈溢出，过程就像下⾯这样：function  foo() {
    throw new Error('SessionStack will help you resolve crashes :)' );
}
function  bar() {
    foo();
}
function  start() {
    bar();
}
start();1
3
5
7
9
JavaScript
function  foo() {
    foo();
}
foo();1
3
JavaScript

281在某个时刻，调⽤堆栈中的函数调⽤次数超过了调⽤堆栈的实际⼤⼩，这样浏览器就会抛出错误的
⾏为，通过看到的错误信息，如下：
代码在单线程中运⾏是相当舒服的，我们不需要去考虑多线程环境下产⽣的⼀些复杂情况，⽐如
死锁
但是单线程运⾏也很收限制，由于JavaScript是单线程的，当它执⾏很慢的该怎么办呢?
当你的在调⽤栈中执⾏函数的花费的时间很⻓会发⽣什么？例如你想在浏览器中⽤JavaScript执⾏⼀些
复杂的图像转化
你也许会问-这也是⼀个问题？ 这问题是当你的调⽤栈中函数在执⾏的时候，浏览器实际上做不了任何
事情-它被阻塞了。这意味着浏览器将不能去渲染，不能够去运⾏其他的任何代码，它卡住了。如果你
想去在你的app中有更流畅体验的这就是个问题并发 & 事件循环

282并且这不是唯⼀的问题。⼀旦你的浏览器开始在调⽤栈中执⾏多个任务，它可能在很⻓⼀段时间内停⽌
响应。并且浏览器会抛出⼀个错误，询问你是否关闭这个⽹⻚。
现在，这不是最好的⽤户体验，是嘛？
那么，我们如何在不阻塞UI和浏览器⽆响应的情况下去执⾏⼀些笨重的代码呢？这个解决⽅式是异步回
调

283